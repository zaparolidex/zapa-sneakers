import { Minus, Plus, ShoppingBag, Trash2, X } from 'lucide-react'; const money=n=>n.toLocaleString('pt-BR',{style:'currency',currency:'BRL'});
export default function CartDrawer({open,onClose,cart,updateQuantity}){const total=cart.reduce((s,i)=>s+i.price*i.quantity,0);return <><div className={`overlay ${open?'show':''}`} onClick={onClose}/><aside className={`cart-drawer ${open?'open':''}`}><div className="cart-head"><div><span>SUA SACOLA</span><h2>{cart.length?`${cart.length} item(ns)`:'Está vazia'}</h2></div><button onClick={onClose}><X/></button></div><div className="cart-list">{!cart.length&&<div className="empty"><ShoppingBag/><p>Seu próximo par favorito pode estar a um clique.</p></div>}{cart.map(i=><div className="cart-item" key={i.key}><img src={i.image} alt={i.name}/><div><strong>{i.name}</strong><span>Tamanho {i.size}</span><b>{money(i.price)}</b><div className="qty"><button onClick={()=>updateQuantity(i.key,i.quantity-1)}>{i.quantity===1?<Trash2/>:<Minus/>}</button><span>{i.quantity}</span><button onClick={()=>updateQuantity(i.key,i.quantity+1)}><Plus/></button></div></div></div>)}</div>{cart.length>0&&<div className="cart-foot"><div><span>Subtotal</span><strong>{money(total)}</strong></div><small>Frete calculado no checkout</small><button>Finalizar compra</button></div>}</aside></>}
